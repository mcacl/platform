server:
  port: 10010
spring:
  cloud:
    nacos:
      server-addr: ${NACOS_HOST:localhost}:${NACOS_PORT:8848}
      config:
        namespace: e169fbcf-c59c-49be-bdac-99162d9a6fc4
  #    datasource:
  #      url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/${MYSQL_DATABASE:platform_core}?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowMultiQueries=true&zeroDateTimeBehavior=CONVERT_TO_NULL
  #      username: ${MYSQL_USERNAME:root}
  #      password: ${MYSQL_PASSWORD:root}
  #      driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    host: ${REDIS_HOST:localhost}
    port: ${REDIS_PORT:6379}
#  kafka:
#    # 指定kafka server的地址，集群配多个，逗号隔开
#    bootstrap-servers: 192.168.1.1:8620
